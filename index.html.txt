<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1">
  <title>LuckSpin – $250k Jackpot</title>
  <script src="https://telegram.org/js/telegram-web-app.js"></script>
  <style>
    body { margin:0; background:#0f0c29; color:#fff; font-family:system-ui; text-align:center; padding:20px; }
    h1 { font-size:48px; background:-webkit-linear-gradient(#00ff9d,#0099ff); -webkit-background-clip:text; -webkit-text-fill-color:transparent; }
    .jackpot { font-size:60px; font-weight:900; color:#00ff9d; margin:30px 0; }
    .btn { background:linear-gradient(#00ff9d,#0099ff); color:#000; padding:18px; border-radius:16px; font-weight:800; font-size:20px; margin:15px 0; }
    .balance { font-size:28px; margin:20px; }
  </style>
</head>
<body>
  <h1>LUCKSPIN</h1>
  <div class="jackpot">$250,000</div>
  <div class="balance">Coins: <span id="c">0</span> SC</div>
  <div class="balance">Entries: <span id="e">0</span></div>
  <div class="btn" onclick="daily()">Daily Bonus → 1000 SC</div>
  <div class="btn" onclick="invite()">Invite Friends → 25,000 SC each</div>
  <div class="btn" onclick="entries()">Buy 100 entries → $10</div>

<script>
  const tg = window.Telegram.WebApp;
  tg.ready();
  const userId = tg.initDataUnsafe.user?.id || 111;
  let data = JSON.parse(localStorage.getItem('ls_'+userId) || '{"c":25000,"e":0,"d":0}');
  
  function save() {
    localStorage.setItem('ls_'+userId, JSON.stringify(data));
    document.getElementById('c').innerText = data.c.toLocaleString();
    document.getElementById('e').innerText = data.e;
  }
  
  function daily() {
    const today = Date.now() - Date.now()%86400000;
    if(data.d < today) {
      data.c += 1000; data.d = today; save();
      tg.HapticFeedback.notificationOccurred('success');
    }
  }
  
  function invite() {
    tg.openTelegramLink('https://t.me/share/url?url=https://'+location.hostname+'/?ref='+userId+'&text=Залетай в LuckSpin – джекпот $250k каждые 30 дней! По моей ссылке сразу +25,000 SC 🔥');
  }
  
  function entries() {
    if(confirm('Buy 100 entries for $10 USDT?')) {
      data.e += 100; save();
    }
  }
  
  // стартовый бонус по рефке
  if(location.search.includes('ref=') && data.c === 25000) {
    data.c += 25000; save();
    setTimeout(()=>alert('🎉 +25,000 SC за рефералку!'),1000);
  }
  
  // красивый рост джекпота
  setInterval(()=>document.querySelector('.jackpot').innerText = '$'+(250000 + Math.floor(Date.now()/1000)%100000).toLocaleString(), 3000);
  
  save();
</script>
</body>
</html>
